name: waiter_mobile
description: "Ritagestion — Application Serveurs (Android/iOS)"
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.40.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../packages/core
  network:
    path: ../../packages/network
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.0.0
  equatable: ^2.0.7
  get_it: ^8.0.3
  path_provider: ^2.1.5
  path: ^1.9.0
  google_fonts: ^6.1.0
  flutter_secure_storage: ^9.2.2
  uuid: ^4.5.1
  mobile_scanner: ^7.2.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

hooks:
  user_defines:
    sqlite3:
      source: sqlite3mc
