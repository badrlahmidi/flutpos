name: ritagestion_tools
description: Scripts utilitaires Ritagestion (simulation cloud, seed, etc.)
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  core:
    path: ../packages/core
  drift: ^2.33.0
  path: ^1.9.0
