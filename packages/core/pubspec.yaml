name: core
description: "Ritagestion — BDD Drift, entités et logique métier partagée"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  drift: ^2.33.0
  path: ^1.9.0
  sqlite3: ^3.3.2
  uuid: ^4.5.1
  bcrypt: ^1.1.3

dev_dependencies:
  drift_dev: ^2.33.0
  build_runner: ^2.4.15
  test: ^1.25.8
