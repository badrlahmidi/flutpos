name: ritagestion_workspace
description: "Ritagestion — Monorepo POS Restaurant Offline-First"
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dev_dependencies:
  melos: ^6.3.0
